import React from 'react';

const Input =(title) => {
    return (
        <div className = "input">
           <input />
           <lable>{title}</lable>
        </div>
    )
}

export default Input;